import * as React from "react";
import {ReactElement} from "react";

export interface ColumnLayoutRendererProps {
    columns: ReactElement[];
}

export class ColumnFlexLayoutRenderer extends React.Component<ColumnLayoutRendererProps> {
    render(): React.ReactElement {
        return undefined;
    }
}

export class Column extends React.Component {
    render(): React.ReactElement {
        return undefined;
    }
}

