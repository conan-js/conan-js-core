export interface Translations {
    existing_user: string;
    register_new_user: string;
}

export let defaultTranslations: Translations = {
    existing_user: 'existing user',
    register_new_user: 'registering new user'
};
